name: Plugin Freeze Guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  plugin-freeze:
    name: Plugin Freeze is currently open
    runs-on: ubuntu-latest
    steps:
      - run: echo "PLUGIN FREEZE OPEN: independent Plugin work is allowed."
