# Theme Tests

No Theme-specific automated tests exist yet.

When Theme tests are introduced, keep them under this directory and run them from a dedicated Theme CI workflow. Theme tests must not be required by the Plugin CI suite.
