# Plugin Tests

These tests belong to the WooGit Main Plugin (`plugin/woogit-backend`).

They cover V1 backend contracts, authorization/security invariants, billing/idempotency, proxy lifecycle, Web login identity, and related Plugin behavior.

They are framework-free contract tests and do not replace integration tests against a real WordPress/WooCommerce test instance.
